name: Demo workflow from youtube

on: workflow_dispatch
jobs:
  print_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GitHub Actions
      - name: Print a few lines 
        run: |
          echo First line!
          echo Second line

